# Topics to Cover

Living checklist for building the Claude Code foundation. Add topics as they come up.
Check items off as they are completed.

## Priority — next session
- [x] **Side-tracked task capture.** Find a way for the user to drop a "side-tracked" task into a checklist for later implementation without interrupting their current work — a quick-capture / inbox mechanism that records the idea and immediately returns to the task in hand. **Start with a `/grill-me` session first thing in the morning** to define the approach (where captured tasks live, how they surface later, how this relates to PROGRESS.md "Deferred / future tasks" and `save-session`) before building anything.
  - Done (2026-06-30): design settled via `/grill-me`. Built `save-for-later` + `implement-task` (skill + command each); edited `continue-project` (surface open tasks) and `wrap-it-up` (narrow task-commit path). Storage is per-project `.claude/checklist.md` (`[ ]` open -> `[x]` done-awaiting-commit -> committed-and-removed by `wrap-it-up`). Static convention check and `code-reviewer` passed. **Live end-to-end test still pending** — needs install into `~/.claude/` then a scratch-project run of the four commands.
  - Update (2026-07-06): storage moved from `.claude/checklist.md` to `docs/CHECKLIST.md` so it is human-navigable outside sessions; `give-feedback` now routes tactical changes into this same file/format (retiring its orphaned docs/PROGRESS.md "Refinement Checklist"), so both feed the `continue-project` -> `implement-task` -> `wrap-it-up` pipeline. Refs updated across `save-for-later`, `implement-task`, `continue-project`, `wrap-it-up`, `new-project`, `give-feedback`.

## Governance and structure
- [x] Governance `CLAUDE.md` (purpose + conventions for building everything)
- [x] Shipped global `~/.claude/CLAUDE.md` drafted at `Claude_MD/global-CLAUDE.md` (lean index: inline non-negotiables + references to Global rules; replaces the current global file on migration)
- [x] Provisioning model: `Global/` and `Stack_specific/` are authoring markers, not preserved at the destination
- [x] Global artifacts installed once into `~/.claude/` (rules flat into `~/.claude/rules/*.md`, skills into `~/.claude/skills/`, agents into `~/.claude/agents/`)
- [x] Language-specific artifacts provisioned per-project into `<project>/.claude/`, language rules linked from project-level `CLAUDE.md` when needed
- [x] Language rules layout: per-language subfolder `<project>/.claude/rules/<lang>/<topic>.md` (avoids filename collisions across languages)
- [x] Update `new-project` to stop copying Global rules into the project and use per-language subfolders
- [x] `MIGRATION.md` authored (install/flatten rules; skills copy as whole folders incl. disclosed files; `Stack_specific/` and the new-project scaffold stay in the store)

## Commands (new artifact type)
- [x] Added `Commands/Global/<name>.md`, one per user-invoked Global skill (`continue-project`, `create-skill`, `give-feedback`, `new-project`, `save-session`, `security-check`, `wrap-it-up`); each invokes its skill, installs to `~/.claude/commands/`. Model-invoked skills (`accessibility`, `codebase-design`, `tdd`) intentionally have none.

## Tooling integrity (meta-skills)
- [x] Fix `new-project` path mapping (table casing, skills source, agents source)
- [x] Audit remaining meta-skills: `continue-project`, `create-skill`, `wrap-it-up`, `give-feedback`, `security-check`, `tdd`
  - [x] `continue-project` audited against `create-skill` rubric: added per-step `Done:` criteria; converted to `## Step N —` headers; collapsed the redundant opening; guarded Step 4 for non-git / no-baseline projects (mirrors `wrap-it-up`); replaced the duplicated copy recipe with delegation to `/new-project`'s Gate 2 activation procedure
  - [x] Portability: removed the hardcoded `C:\Claude\...` store path from `continue-project` and `new-project`; store root is now prompted once and persisted as `storeRoot` in `.claude/wrap-it-up.json` (`wrap-it-up` preserves the key on rewrite)
  - [x] Description sweep: added `description` frontmatter to `continue-project`, `new-project`, `create-skill` (now all five user-invoked meta-skills carry a human-facing summary)
  - [x] Built `implement-milestone` skill (`Skills/Global/implement-milestone`) + command via the `create-skill` procedure: enacts `development-workflow.md` (scope-what-applies gate -> research -> plan -> red-green TDD -> review), stops at the commit gate, and branches the closer (`/wrap-it-up` if done-criteria met, else `/save-session`). `continue-project` recommends it from both the first-session and clean-state reports, and `new-project`'s closing recommends it as the first step on a fresh project. All cross-references verified to resolve
  - [x] Audited the remaining five against the `create-skill` Step 4 rubric (`create-skill`, `wrap-it-up`, `give-feedback`, `security-check`, `tdd`). `create-skill` is exemplary (defines the rubric); `tdd` (discipline/loop), `security-check` (audit/checklist), and `wrap-it-up` (complex completion, just reviewed) are well-formed for their type — per-step `Done:` would be artificial or high-risk. Fixed `give-feedback`: it was a linear procedure with bold-numbered steps but no completion criteria, so converted `**N. Title**` -> `## Step N —` with a `Done:` line each, matching its audited siblings (its frontmatter `description` + H1 were already fixed under the Skills audit).
- [x] Verify every artifact referenced by a meta-skill actually exists — swept all Global SKILL.md cross-references. Slash commands (`/new-project`, `/implement-milestone`, `/implement-task`, `/wrap-it-up`, `/save-session`; `/clear` is built-in), the four Global rules (`development-workflow`, `agents`, `testing`, `code-review`), all disclosed companion files, `continue-project`'s "Gate 2, steps 1-4" delegation, and every skill (24) + agent (23) + ruleset dir (19) named in `new-project`'s tech-choice table all resolve. No broken references.

## ECC removal and cross-references
- [x] Rule recorded in CLAUDE.md: no ECC bleed (naming, `origin:`, content, cross-references)
- [x] Remove `origin: ECC` frontmatter from all 30 skill files
- [x] Fix clear ECC-naming cross-refs: `angular-developer` (`tdd-workflow`->`tdd`, `security-review`->`security-check`), `react-patterns` (removed `/react-*` commands line)
- [x] Repoint missing-agent references to what exists: `tdd-guide`->`tdd` skill (agents.md, testing.md, development-workflow.md, typescript-build-resolver.md); `e2e-runner`->`e2e-testing` skill (typescript/testing.md); `refactor-cleaner`->`code-simplifier`; dropped non-existent rows from agents.md and added the existing `code-simplifier`
- [x] Add `accessibility` skill to `Skills/Global/` (scrubbed of ECC origin + dead Related links); restored a11y references in `react-testing` and `react-patterns`
- [x] Repoint `security-review`->`security-check` (backend-patterns, nuxt/security.md); dropped `api-design` reference in backend-patterns
- [x] Emoji audit: removed `✅`/`⏭️` from `wrap-it-up`; storewide sweep found no other genuine emojis (arrows and box-drawing tree characters are technical notation, kept)
- [x] Add `react-performance` skill to `Skills/Stack_specific/` (scrubbed: dropped `nextjs-turbopack` link, repointed `accessibility` to the global skill, removed commands line); resolves the `react-patterns`/`react-testing` references
- [x] `api-design`: no references remain in the store — no action needed (per decision)
- [x] `nextjs-turbopack`: not added (mostly version trivia). Folded the durable `proxy.ts` middleware rule + Turbopack-default note into `react-patterns`; removed the `vite-patterns` reference. Also fixed a stale `accessibility` path in `react-patterns`
- [x] Build `save-session` skill (`Skills/Global/save-session`): per-project `.claude/sessions/` handoff file, user-invoked, no PROGRESS.md/git side effects, write-and-report (confirm only if ambiguous). Design reached via `/grill-me`. Integrated: `continue-project` detects + suggests resume; `wrap-it-up` deletes on completion (lists/asks if multiple). Also fixed `continue-project`'s `Language Skills`/`Agents\Language Specific` path bugs (same defect as `new-project`)
- [x] Live-test `save-session` (create-skill Step 4): ran it on this session; wrote `.claude/sessions/2026-06-29-framework-foundation-session.md`, captured evidence honestly (untested skills went under "Not Tried", not "Worked"), did not prompt since state was unambiguous. Passed.

## Rules
- [x] Review each `Rules/Global/` file for correctness and no internal contradictions — all 10 read. Internally and cross-rule consistent (the `<50`/`<800`/`<4` thresholds match between `coding-style` and `code-review`; the 80% + RED-GREEN-REFACTOR workflow matches across `testing`/`code-review`/`development-workflow`; all agent references resolve; `tdd` skill used throughout, no `tdd-guide` sediment). Fixed one ECC-bleed defect: `development-workflow.md` link label `[common/git-workflow.md]` (the `common/` folder does not exist in this store) -> `[git-workflow.md]`.
- [x] Define and document rule precedence (Global vs Stack_specific vs user) — documented in the always-loaded shipped global CLAUDE.md (`Claude_MD/global-CLAUDE.md` lines 14-15: "user instructions, then the project `CLAUDE.md`, then this global file. Language-specific rules override general rules"), consistent with governance `CLAUDE.md`. Stated once in the always-loaded file by design (restating per-rule would violate DRY).
- [x] Cross-link related rules consistently — standardized cross-link style to bare relative form (`[x.md](x.md)`; dropped the `./` prefix from the `development-workflow`/`git-workflow` pair) and added a `## Related rules` footer to the six rules that had genuine coupling but no links (`coding-style`, `testing`, `security`, `agents`, `patterns`, `performance`) plus `hooks`. All 10 now cross-link; `code-review`'s existing "Integration with Other Rules" section kept.

## Agents
- [x] Frontmatter consistency audit across all agents (name, description, tools, model) — all 30 agents (5 Global, 25 Stack_specific) carry the four fields; names kebab-case matching filenames. Added explicit trigger language to four ECC-adopted descriptions that stated only WHAT: `fastapi-reviewer`, `flutter-reviewer`, `kotlin-reviewer`, `code-simplifier`.
- [x] Confirm tool grants are minimal and correct per agent — reviewers read-only + Bash (`Read`/`Grep`/`Glob`/`Bash`); build-resolvers and remediation agents (`security-reviewer`, `code-simplifier`) add `Write`/`Edit`; `planner`/`architect` read-only, no Bash. All fit role.
- [x] Confirm every agent has the Prompt Defense Baseline section — present in all 30.

## Skills
- [x] Frontmatter and structure consistency audit (folder = name = H1) — all 14 Global skills checked. Fixed `give-feedback` (added missing `description`; H1 `# Feedback` -> `# Give Feedback`). Kept two deliberate cases: `tdd` -> `# Test-Driven Development` (universal acronym, name referenced across rules) and `accessibility` -> `# Accessibility (WCAG 2.2)` (leading word matches, parenthetical adds version scope). `give-feedback`'s `**1. Collect**` step format (vs `## Step N —`) deferred to the meta-skill audit (line 25, which lists it).
- [x] Activation/trigger correctness (fires when intended, quiet otherwise) — 10 user-invoked skills carry `disable-model-invocation: true` + paired command (quiet unless commanded); 4 model-invocable skills (`codebase-design`, `tdd`, `accessibility`, `grill-me`) omit the flag and carry "Use when..." triggers. Command coverage exact: 11 commands = 10 user-invoked + `grill-me`; 3 model-only skills correctly have none.
- [x] Merge ECC + Matt Pocock TDD into global `tdd` skill (folded in RED/GREEN gates; framework code stays in language testing skills, not duplicated)
- [x] Add Matt Pocock `codebase-design` skill to `Skills/Global/` (SKILL + DEEPENING + DESIGN-IT-TWICE; CONTEXT.md refs adapted to CLAUDE.md/ADRs); restored the `tdd` skill's deep-modules reference to it
- [x] Fix ECC sediment cross-references in `react-testing` (removed non-existent `accessibility` skill + `tdd-guide` agent + `/react-test` commands; `tdd-workflow` link now points to global `tdd`). `e2e-testing` checked — no broken references
- [x] Add Next.js API route-handler integration test pattern to `react-testing` (gap not covered by component-focused tests)
- [x] Fix `create-skill` file-location path (now points at `Skills/Global/` / `Skills/Stack_specific/`; notes skills are authored here, not installed until provisioned)
- [x] Rework `create-skill` into pointer architecture: lean procedure points to disclosed `writing-great-skills.md` + `GLOSSARY.md`; `Built-in SKILL.md` content merged then deleted. Default output path now `~/.claude/skills/<name>/`; added a slash-command step and a command-aware test step
- [x] Convert `wrap-it-up` model-invoked -> user-invoked (`disable-model-invocation`, trigger phrasing stripped); paired command added
- [x] Co-locate the project `CLAUDE.md` scaffold at `Skills/Global/new-project/project-CLAUDE.md`; `new-project` references it relatively instead of an inline template

## Coverage parity per language
- [x] Define the standard set per language (rules + patterns + testing + reviewer + build-resolver) — defined in `COVERAGE.md` (new, store root), with the full per-language matrix and a build-on-demand gap policy; referenced from the `CLAUDE.md` Definition of Done.
- [x] Resolve known gaps: Perl (rules exist, no `new-project` row), Ruby and ArkTS (no skills/agents), Vue/Nuxt (no dedicated reviewer agent) — Fixed three accidental wiring defects in `new-project`'s table: `typescript-reviewer` and `swift-reviewer` existed but were unprovisioned (now wired), and Perl had no row (added with its existing skills). Decisions: Angular/Vue/Nuxt now share `typescript-reviewer` (TS stacks; no framework-specific reviewer by design); Ruby and ArkTS stay rules-only as intentional minimal support; remaining partial gaps (Java/C#/C++/F#/PHP/Dart/Python/FastAPI patterns-or-testing-or-build-resolver) documented as intentional in `COVERAGE.md` rather than built speculatively — the reviewer (highest-leverage artifact) already exists for nearly all, and a thin patterns/testing skill would fail the `create-skill` justification test.

## Conventions
- [x] Document frontmatter schemas for agents and skills — tightened the governance `CLAUDE.md` "Conventions" subsections from prose into explicit per-field schemas: agents require `name`/`description`/`tools` (array)/`model` (one of `opus`/`sonnet`/`haiku`); skills require `name`/`description` with optional `disable-model-invocation: true`, and the description form differs by invocation type (trigger conditions vs human-facing summary).
- [x] Enforce naming conventions across the whole store — swept all artifact types: 30 agent files (file = `name`, kebab), 14 Global + 32 Stack_specific skill folders (folder = `name`, kebab), 11 commands (kebab, match skills), all rule files (kebab topics). The `Stack_specific` marker is canonical everywhere — no live wrong-form path token (every hit of a hyphen/space form is adjectival English or a historical note quoting an already-fixed bug). Disclosed reference docs (`GLOSSARY.md`, `DEEPENING.md`, `DESIGN-IT-TWICE.md`, `project-CLAUDE.md`) are intentionally non-kebab and not artifact names. No live violations.

## Superpowers adoption (reviewed 2026-06-30)
Critical review of the `superpowers` plugin (`claude_ruleset/superpowers/6.0.3`) against this store.
Most of its 13 process skills are already covered (TDD, brainstorming, plan writing/execution,
requesting review, branch finishing, skill authoring). The items below are the genuine gaps and
worthwhile techniques. Everything adopted must be rewritten to this store's conventions: English,
no emojis, no `superpowers:`/`docs/superpowers/` branding, no narrative session numbers,
language-agnostic in Global skills.
- [x] **systematic-debugging skill (NEW) — the one clear gap.** Built `Skills/Global/systematic-debugging/` (2026-06-30): model-invoked discipline skill mirroring `tdd` (no command, per the model-invoked-skills convention). `SKILL.md` carries the four-phase method (root-cause investigation -> pattern analysis -> single-hypothesis testing -> fix at source via the `tdd` RED-GREEN gates), the "3+ failed fixes = question the architecture" rule, rationalization table, and red flags. Three disclosed companions added and rewritten language-agnostic and de-branded: `root-cause-tracing.md`, `defense-in-depth.md`, `condition-based-waiting.md` (graphviz diagrams, TS-specific code, and narrative session stats removed; concrete polling helpers deferred to language testing skills). Wired into `development-workflow.md` step 2 and cross-linked bidirectionally with `tdd`. All relative cross-references verified to resolve. **Live pressure-test pending** (create-skill Step 6): needs install into `~/.claude/` then a fresh-session debugging scenario to confirm it fires and holds the root-cause-first discipline.
- [x] **receiving-code-review discipline.** Added a "Receiving Review Feedback" section to `Rules/Global/code-review.md` (2026-06-30): response pattern (read -> restate -> verify against codebase -> evaluate -> respond -> implement one at a time, testing each); no-performative-agreement table; clarify-all-unclear-before-implementing; verify external feedback harder than the user's; YAGNI-check on "implement properly" suggestions; explicit when-to-push-back list; graceful self-correction. Tied to the existing severity levels and the `tdd` failing-test-first discipline for confirmed bugs. Rule-shaped (standard + tables), de-branded, no emojis. Distinct from `give-feedback` (which routes end-user feedback to requirement changes, not code-review reception).
- [x] **Skill-testing methodology.** (2026-06-30) Reframed `create-skill` Step 6 as TDD-for-skills, split by skill type (technique/reference = self-test; discipline = baseline-without-skill first, then pressure-test); also handled the model-invoked no-command case. Added disclosed companion `Skills/Global/create-skill/testing-skills.md`: RED-GREEN-REFACTOR cycle, pressure-scenario construction (pressure-types table, key elements), micro-testing wording against a no-guidance control (5+ reps, read each match, variance as signal), loophole-closing, meta-testing, done-criteria. Added "Matching form to failure" to `writing-great-skills.md` (prohibition vs positive recipe vs structural slot vs conditional; no nuance clauses; exemptions do not scope). Single-source split: form choice in `writing-great-skills`, verification method in `testing-skills`; all sibling cross-references resolve. De-branded, no emojis, no narrative stats, language-agnostic.
- [x] **subagent-driven-development techniques (cherry-pick, do not adopt whole).** (2026-06-30) Added a "Working with subagents efficiently" section to `Rules/Global/agents.md` with both techniques: file-based handoffs (hand agents a path, not pasted text; have them write bulk results to a file and return a summary plus path) and durable progress across compaction (record completed/committed work in `PROGRESS.md`/checklist/git, and after a compaction trust those over recollection — never redo or re-dispatch completed work). Placed in `agents.md` rather than the two implement skills so it is single-sourced: both `implement-task` and `implement-milestone` already declare `agents.md` their source of truth, so they inherit it with no skill edits. Did not adopt the rest of superpowers' subagent-driven-development (its fresh-subagent-per-task orchestration conflicts with Lindholm's in-session gate model).
- [x] **verification rationalization table.** (2026-06-30) Added a "Verification Before Completion" section to `Rules/Global/development-workflow.md`: the four-step gate (identify the proving command -> run fresh and full -> read the output -> claim only if confirmed), a rationalization table ("should work now" -> run it; "agent said success" -> verify independently; stale run does not count), and red flags. Framed as the single-source standard that the implementation skills' "confirmed with evidence, not assumed" phrase already applies at their gates. No standalone skill created, as recommended. De-branded (dropped the "failure memories"/"you'll be replaced" framing), no emojis.
- [x] **dispatching-parallel-agents.** (2026-06-30) Expanded the thin "Parallel Task Execution" section in `agents.md`: stated the parallelism mechanic (multiple agent calls in one message run concurrently; one per message is sequential), how to scope each agent (focused scope, standalone context, specific expected output, cross-linked to the file-handoff guidance), an explicit when-NOT-to-parallelize list (shared state, related failures, needs full-system context), and a check-on-return step (verify changes do not conflict, integrate, run the full suite). Replaced the absolute "ALWAYS parallelize" framing with the conditional. Tight addition, no bloat; cross-references the item-4 section rather than duplicating it.
- [x] **using-git-worktrees / finishing-branch worktree mechanics — SKIPPED BY DECISION (2026-06-30).** Not adopted, deliberately. Worktrees pay off mainly when many parallel agents mutate files simultaneously and need filesystem isolation; Lindholm delegates review to agents but implements in the main session, so that scenario does not arise. Branch isolation and closure are already owned by `git-workflow.md` and `wrap-it-up`; adding worktree mechanics would overlap and risk contradiction. Fails the `create-skill` justification test at the current scale. Revisit only if heavy parallel file-mutating agents become part of the workflow.


## Framework audit sweep (2026-07-07)
Full-store review (agents, skills, rules, commands, docs) surfaced four defects; all fixed:
- [x] **Broken `common/` cross-references in stack rules (CRITICAL).** All ~104 stack-specific rule files opened with a markdown link to a non-existent `../common/<topic>.md` (a template error that cascaded to every language folder). Since Global and stack rules install to different roots, no relative link can resolve at the destination — replaced every link with prose ("extends the global `<topic>.md` rule"), which is correct in-store and post-install. Only remaining `common/` hits are legitimate (`@angular/common` imports, Rust `tests/common/` convention).
- [x] **`build-error-resolver` renamed to `typescript-build-resolver`.** The agent had a generic name but purely TypeScript/JS content, and `COVERAGE.md` used it both as "the generic fallback" and as the TypeScript resolver. Renamed file, frontmatter `name`, description, and H1 to be honestly TS/JS-scoped; `COVERAGE.md` gap policy now names the main-loop build workflow as the generic fallback, and the Angular/Vue/Nuxt/web matrix rows mark it `shared (typescript-build-resolver)`. References updated in `Rules/Global/agents.md`, `Rules/Global/performance.md`, and `new-project`'s tech-choice table. Installed `~/.claude/agents/` copy needs re-install per `MIGRATION.md`.
- [x] **`Language_specific/` renamed to `Stack_specific/`.** The folder held language, framework, and infrastructure artifacts (docker, postgres, deployment), so the old name misdescribed it. Renamed across `Agents/`, `Rules/`, `Skills/`; governance `CLAUDE.md` now defines `Stack_specific/` by install destination (per-project, scoped to languages, frameworks, or infrastructure choices) and marks `Language_specific` as a retired form. `COVERAGE.md` gained a named "Infrastructure skills (stack-agnostic, library-attached)" category (`docker-patterns`, `deployment-patterns`, `database-migrations`, `e2e-testing`, `postgres-patterns`, `redis-patterns`). All path references swept (`MIGRATION.md`, `new-project`, `continue-project`, this file).
- [x] **Skill naming convention documented.** Governance `CLAUDE.md` Skills section now defines the four sanctioned `Stack_specific/` name forms (`<language>-patterns`/`-testing`, `<tool>-patterns`, `<framework>-developer`, `<language>-<idiom>`) and declares names outside them defects.

## Interaction design — next session
- [x] **`resources/` folder scope.** Warning was a model misread: it confused design inspiration files (raw input, `resources/`) with `docs/DESIGN.md` (authored design foundation output). Fixed by adding "design references" to the layout diagram parenthetical and updating the `resources/README.md` template to explicitly state the raw-input vs. authored-output boundary.
- [x] **Global question and flagging alignment.** Defined four interaction types (disambiguation, confirmation gate, stopping question, recommendation) in a new `## Interaction design` section added to the global CLAUDE.md (store draft + installed copy). Rules: no informational flags, always bullet/number multiple items, (Y/N) both caps, one question at a time, every message ends with an explicit closing prompt. Fixed (y/N) → (Y/N) in wrap-it-up. No separate rule file needed — the standard lives in the always-loaded CLAUDE.md.
- [x] **`wrap-it-up` commit interaction.** Replaced the jargon-heavy single-line git plan with a structured format: heading names the milestone or task count, "Commit message(s):" bulleted, "Git actions:" bulleted in plain language, closed with "Proceed? (Y/N)". Updated the Confirmation Model and Git Ritual sections in wrap-it-up accordingly.
